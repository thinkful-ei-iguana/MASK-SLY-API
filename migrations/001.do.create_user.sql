CREATE TABLE "user" (
  "id" SERIAL PRIMARY KEY GENERATED BY DEFAULT AS INTEGER,
  "Username" TEXT NOT NULL UNIQUE,
  "Password" TEXT NOT NULL,
  "First_Name" TEXT NOT NULL,
  "Last_Name" TEXT NOT NULL,
  "Email" TEXT NOT NULL,
);